const fileInput=document.querySelector("#file"),markdown=document.querySelector("#markdown"),dropzone=document.querySelector("#dropzone"),wordButton=document.querySelector("#convert"),pdfButton=document.querySelector("#convert-pdf"),status=document.querySelector("#status");let name="document";
async function load(file){if(!file)return;name=file.name.replace(/\.md$/i,"")||"document";markdown.value=await file.text();status.textContent=`${file.name} loaded · ${markdown.value.length.toLocaleString()} characters`}
fileInput.addEventListener("change",()=>load(fileInput.files[0]));["dragenter","dragover"].forEach(type=>dropzone.addEventListener(type,event=>{event.preventDefault();dropzone.classList.add("drag")}));["dragleave","drop"].forEach(type=>dropzone.addEventListener(type,event=>{event.preventDefault();dropzone.classList.remove("drag")}));dropzone.addEventListener("drop",event=>load(event.dataTransfer.files[0]));
async function convert(format){if(!markdown.value.trim()){status.textContent="Paste Markdown or choose a file first.";markdown.focus();return}wordButton.disabled=true;pdfButton.disabled=true;status.textContent=`Building your ${format.toUpperCase()} document…`;try{const response=await fetch(format==="pdf"?"/api/convert-pdf":"/api/convert",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({markdown:markdown.value,filename:name})});if(!response.ok)throw new Error((await response.json()).error);const url=URL.createObjectURL(await response.blob()),link=Object.assign(document.createElement("a"),{href:url,download:`${name}.${format}`});link.click();URL.revokeObjectURL(url);status.textContent=`${name}.${format} created successfully.`}catch(error){status.textContent=error.message||"Conversion failed."}finally{wordButton.disabled=false;pdfButton.disabled=false}}
wordButton.addEventListener("click",()=>convert("docx"));pdfButton.addEventListener("click",()=>convert("pdf"));